services:
  - type: web
    name: steelbeam-spark
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: dist/client
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
